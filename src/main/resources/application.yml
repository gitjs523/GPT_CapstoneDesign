spring:
  application:
    name: Snow
  config:
    import: optional:file:.env[.properties]
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:snow}
    username: ${POSTGRES_USER:snow}
    password: ${POSTGRES_PASSWORD:snow-dev-password}
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
  sql:
    init:
      mode: never

server:
  port: ${SERVER_PORT:8080}
